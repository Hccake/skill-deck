# 允许连接流程按需唤起 WSL 发行版

Skill Deck 继续发现已安装的 WSL 发行版，并允许用户选择或使用某个 WSL Environment 时，由连接流程或一次受控重连按需唤起已经停止的发行版。当前 WSL CLI 没有可靠的“执行但禁止启动”模式；在每次读取前额外检查运行状态既会增加进程调用和实现成本，也无法消除检查与实际读取之间的状态变化，因此不采用只显示正在运行的发行版或读取前预检查方案。Skill Deck 仍不提供单独的启动、停止、重启、安装或注销功能。
