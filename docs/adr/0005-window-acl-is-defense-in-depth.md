---
status: accepted
---

# 窗口 ACL 作为纵深防御

窗口 ACL 采用默认拒绝策略，用于减少误调用风险并限制 WebView 能力。业务授权、路径安全、修订号和数据归属仍由 Backend 统一校验。

主窗口和安装向导共享同一进程与用户权限，因此窗口身份只决定可以进入哪些命令入口，不构成独立的业务信任域。窗口开始加载不受信任的内容、运行第三方扩展或使用不同的操作系统身份时，必须重新评估这项边界。当前行为由[系统架构](../architecture.md#窗口命令权限)负责说明，变更同步要求见[贡献指南](../../CONTRIBUTING.md)。
